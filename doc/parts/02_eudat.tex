\label{sec:eudat}
\ednote{TODO: Write this}
% \begin{lstlisting}
% # create a new graph
% G = nx.DiGraph()
% 
% # read current parameters
% A = self.A
% N = self.N
% 
% # add all the edges
% for i in range(N):
% 		for j in range(N):
% 				if A[i, j] != 0:
% 						G.add_edge(i, j, weight=A[i,j])
% 
% # and return the graph
% return G
% \end{lstlisting}